import 'package:genui/genui.dart';

/// Returns the default widget catalog for clawfree.
/// Uses genUI core widgets (Text, Button, TextField, Card, etc.)
Catalog getClawfreeCatalog() => CoreCatalogItems.asCatalog();
